const PHOTOS=[1,2,3,4,5,6,7,8,9].map(n=>`assets/foto${n}.jpg`);
const PHRASES=["Allyson, eres mi lugar favorito","Mi mundo florece contigo","Te elegiría una y mil veces","Siempre tú, siempre nosotros","Eres mi casualidad favorita","Contigo, hasta lo simple se vuelve recuerdo","Tu sonrisa ilumina mi universo","Cada momento contigo vale oro","Mis flores amarillas siempre llevarán tu nombre","Gracias por coincidir conmigo"];
const orbit=document.querySelector('#orbit');let rot=0,zoom=1,drag=false,lastX=0;
PHOTOS.forEach((src,i)=>{let c=document.createElement('div');c.className='card';c.innerHTML=`<img src="${src}">`;c.dataset.a=(i/PHOTOS.length)*Math.PI*2;orbit.appendChild(c)});
PHRASES.forEach((t,i)=>{let p=document.createElement('div');p.className='phrase';p.textContent=t;p.dataset.a=(i/PHRASES.length)*Math.PI*2+.25;orbit.appendChild(p)});
function layout(){const w=innerWidth,h=innerHeight,cx=w/2,cy=h*.47,rx=Math.min(w*.43,310)*zoom,ry=Math.min(h*.29,235)*zoom;document.querySelectorAll('.card,.phrase').forEach(e=>{let a=+e.dataset.a+rot,r=e.classList.contains('phrase')?1.18:1,x=cx+Math.cos(a)*rx*r,y=cy+Math.sin(a)*ry*r,z=(Math.sin(a)+1)/2;e.style.left=x+'px';e.style.top=y+'px';e.style.transform=`translate(-50%,-50%) scale(${.72+z*.35})`;e.style.opacity=.42+z*.58;e.style.zIndex=Math.floor(z*10)+2})}layout();addEventListener('resize',layout);
scene.onpointerdown=e=>{drag=true;lastX=e.clientX;scene.setPointerCapture(e.pointerId)};scene.onpointermove=e=>{if(!drag)return;rot+=(e.clientX-lastX)*.006;lastX=e.clientX;layout()};scene.onpointerup=()=>drag=false;scene.onwheel=e=>{zoom=Math.max(.65,Math.min(1.35,zoom-e.deltaY*.001));layout()};
startBtn.onclick=()=>start.classList.add('hide');letterBtn.onclick=()=>letter.classList.add('show');close.onclick=()=>letter.classList.remove('show');letter.onclick=e=>{if(e.target===letter)letter.classList.remove('show')};music.onclick=()=>alert("Aquí podemos poner la canción especial de ustedes 💛");
const canvas=document.querySelector('#galaxy'),renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));const sc=new THREE.Scene(),cam=new THREE.PerspectiveCamera(60,innerWidth/innerHeight,.1,1000);cam.position.z=5;function resize3(){renderer.setSize(innerWidth,innerHeight);cam.aspect=innerWidth/innerHeight;cam.updateProjectionMatrix()}resize3();addEventListener('resize',resize3);const geo=new THREE.BufferGeometry(),N=1800,pos=new Float32Array(N*3);for(let i=0;i<N;i++){let r=3+Math.random()*13,a=Math.random()*Math.PI*2,z=(Math.random()-.5)*12;pos[i*3]=Math.cos(a)*r;pos[i*3+1]=Math.sin(a)*r;pos[i*3+2]=z}geo.setAttribute('position',new THREE.BufferAttribute(pos,3));const mat=new THREE.PointsMaterial({color:0xffd84d,size:.035,transparent:true,opacity:.8});const stars=new THREE.Points(geo,mat);sc.add(stars);(function anim(){requestAnimationFrame(anim);stars.rotation.z+=.00035;stars.rotation.y+=.00012;renderer.render(sc,cam)})();